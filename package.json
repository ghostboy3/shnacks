{
  "name": "shnacks2",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.2",
    "pdf-parse": "^1.1.1",
    "openai": "^4.20.1",
    "dotenv": "^16.3.1"
  }
}
